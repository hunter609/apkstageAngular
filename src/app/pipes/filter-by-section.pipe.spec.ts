import { FilterBySectionPipe } from './filter-by-section.pipe';

describe('FilterBySectionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBySectionPipe();
    expect(pipe).toBeTruthy();
  });
});
