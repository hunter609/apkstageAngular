import { FilterByDivisionPipe } from './filter-by-division.pipe';

describe('FilterByDivisionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByDivisionPipe();
    expect(pipe).toBeTruthy();
  });
});
